/**
 * Shared constants used across the Slack formatter codebase
 */

/**
 * Default values for user handling
 */
export const USER_CONSTANTS = {
  /** Default username for unidentified users */
  UNKNOWN_USER: 'Unknown User',
} as const;
